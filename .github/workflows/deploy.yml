name: Deploy to VPS and AWS

on:
  push:
    branches: ["main"]

jobs:
  deploy_vps:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            set -e

            cd /var/www/beapp_jyknow_usr/data/www/beapp.jyknow.fi

            git fetch --all
            git reset --hard origin/main

            npm ci
            npm run build

            export PM2_HOME=/var/www/beapp_jyknow_usr/data/.pm2

            pm2 reload ai-visibility-server || pm2 start dist/index.js --name ai-visibility-server
            pm2 save

  deploy_aws:
    runs-on: ubuntu-latest
    needs: deploy_vps
    steps:
      - name: Deploy to AWS EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USER }}
          key: ${{ secrets.VPS_SSH_KEY_AWS }}
          port: ${{ secrets.AWS_PORT || 22 }}
          script: |
            set -e

            cd /home/ec2-user/beapp-server

            git fetch --all
            git reset --hard origin/main

            npm ci
            npm run build

            export PM2_HOME=/home/ec2-user/.pm2

            pm2 reload ai-visibility-server || pm2 start dist/index.js --name ai-visibility-server
            pm2 save
